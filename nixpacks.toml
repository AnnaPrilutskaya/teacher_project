[phases.setup]
nixPkgs = ["gcc", "postgresql", "python39"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install -r requirements.txt"
]

[start]
cmd = "python manage.py migrate && gunicorn teacher.wsgi:application --bind 0.0.0.0:$PORT"